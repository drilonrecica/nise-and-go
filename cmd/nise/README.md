# cmd/nise

Entry point for the `nise` CLI binary. Installed as `github.com/drilonrecica/nise-and-go/cmd/nise`.

Contains only `main` and process-level concerns (exit codes, signal wiring). Command logic lives in `internal/cli`.

Populated by Slice 1 (M1-001).
