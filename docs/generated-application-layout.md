# Generated Application Layout

This page describes the tree `nise new` produces. It is the layout of an application built with Nise, not the layout of the Nise repository itself — that is [Repository layout](repository-layout.md).

The decision and the alternatives rejected are in [ADR 0009](adr/0009-generated-application-layout.md). This page describes planned V0.1 behavior; see [What Slice 1 generates](#what-slice-1-generates) for what exists today.

`<app>` is the application's name and `<feature>` is one feature package.

## Ownership markers

Every generated file follows one of the two ownership models in [ADR 0003](adr/0003-application-ownership.md).

| Marker | Meaning |
|---|---|
| `[nise]` | Nise owns the whole file. It is regenerated, and hand edits are lost. |
| `[app]` | The application owns the whole file. Nise writes it once and never overwrites it. |

Generated files declare the same thing in a header comment:

```text
Code generated by nise; DO NOT EDIT.                                            → [nise]
Generated once by nise; owned by this application. Nise will not overwrite it.  → [app]
```

Neither header records a Nise version. A version in the header would rewrite every file on every CLI upgrade and defeat the clean-regeneration check.

Ownership is also readable from the path. The directory names `store/`, `openapigen/`, and `embedded/`, and any file ending `.gen.go`, are regenerated output.

## Tree

```text
<app>/
├── AGENTS.md                          [nise]  static instructions for external coding tools
├── README.md                          [app]   the application's own readme
├── nise.json                          [nise]  project recipe: profile, modules, versions (ADR 0010)
├── go.mod                             [app]   application module; the application may add dependencies
├── go.sum                             [app]
├── sqlc.yaml                          [nise]  one sql block per feature; regenerated as features are added
├── .gitignore                         [app]
│
├── api/
│   └── openapi.yaml                   [app]   authoritative transport contract; hand-edited from here on
│
├── cmd/
│   └── <app>/
│       └── main.go                    [app]   flag parsing and mode selection; calls internal/app
│
├── db/
│   ├── embed.go                       [nise]  package db; embeds migrations/*.sql into the binary
│   └── migrations/
│       └── 00001_init.sql             [app]   readable SQL, sequential prefix, applied by nise db migrate
│
├── deploy/
│   ├── Dockerfile                     [app]   minimal non-root runtime image
│   ├── compose.yaml                   [app]   local PostgreSQL and supporting services
│   └── README.md                      [app]
│
├── frontend/                          SvelteKit project; contains no Go files
│   ├── README.md                      [app]   states that pnpm build writes into internal/platform/webui
│   ├── package.json                   [app]
│   ├── pnpm-lock.yaml                 [app]   committed
│   ├── svelte.config.js               [app]   adapter-static output path; see Embedding below
│   ├── vite.config.ts                 [app]
│   ├── tsconfig.json                  [app]
│   ├── static/                        [app]   favicon and other served-as-is files
│   └── src/
│       ├── app.html                   [app]
│       ├── app.css                    [app]   Tailwind entry and design tokens
│       ├── routes/
│       │   ├── (app)/                 [app]   authenticated route group; never prerendered
│       │   └── (public)/              [app]   sign-in and other unauthenticated routes
│       └── lib/
│           ├── api/
│           │   ├── schema.d.ts        [nise]  openapi-typescript output
│           │   └── client.ts          [app]   small typed fetch wrapper over schema.d.ts
│           ├── components/            [app]   application-owned UI components
│           └── features/<feature>/    [app]   feature-local stores, helpers, and components
│
└── internal/
    ├── app/
    │   ├── app.go                     [app]   explicit constructor wiring; no container, no registry
    │   └── modes.go                   [app]   all, web, and worker process modes
    │
    ├── features/
    │   └── <feature>/
    │       ├── domain.go              [app]   domain types and rules
    │       ├── usecase.go             [app]   owns the transaction boundary
    │       ├── handler.go             [app]   transport: decode, call use case, translate result
    │       ├── queries/
    │       │   └── <feature>.sql      [app]   hand-written SQL, input to sqlc
    │       ├── store/                 [nise]  sqlc output for this feature
    │       └── <feature>_test.go      [app]
    │
    └── platform/
        ├── config/                    [app]   typed configuration over runtime/config
        ├── database/                  [app]   pgxpool setup and the Goose migration runner
        ├── httpapi/
        │   ├── router.go              [app]   middleware order and the /api/v1 mount
        │   └── openapigen/            [nise]  oapi-codegen strict chi server types and bindings
        ├── mail/
        │   ├── mail.go                [app]
        │   └── templates/             [app]   embedded text/template and html/template messages
        └── webui/
            ├── webui.go               [nise]  embeds the built frontend and serves the SPA
            └── embedded/
                ├── placeholder.html   [nise]  committed; makes go build work before pnpm build
                └── client/            [nise]  pnpm build output; not committed
```

## Where things live and why

**Three top-level artifact trees.** `api/`, `db/`, and `frontend/` hold the artifacts a contributor edits without writing Go — the OpenAPI document, the SQL migrations, and the Svelte application. Everything Go-shaped is under `cmd/` and `internal/`.

**`internal/` has exactly three children.** `internal/app/` wires, `internal/features/` holds the business, `internal/platform/` holds technical infrastructure. A domain rule in `internal/platform/` is in the wrong place. There is no `internal/util/`, `internal/common/`, `internal/shared/`, or `internal/pkg/`.

**A feature is a vertical slice.** `internal/features/invoice/` owns its handlers, use cases, SQL, domain rules, and tests. Its frontend half is `frontend/src/routes/(app)/invoices/` plus `frontend/src/lib/features/invoice/`.

**Transactions.** The use case opens and commits. Handlers do transport work. The `store` package runs queries and never begins a business transaction on its own.

## Embedding

One binary carries the migrations, the mail templates, and the built frontend. `go:embed` cannot reach outside the directory of the package that declares it, so each embed point is an ancestor of what it embeds:

| Embedded | Declared in | Pattern |
|---|---|---|
| Migrations | `db/embed.go` | `migrations/*.sql` |
| Mail templates | `internal/platform/mail/mail.go` | `templates` |
| Built frontend | `internal/platform/webui/webui.go` | `all:embedded` |

The frontend is the one that needs explanation. `frontend/` is a pnpm project with no Go in it, so the static adapter writes its output into the Go package instead:

```js
// frontend/svelte.config.js
adapter({
  pages: '../internal/platform/webui/embedded/client',
  assets: '../internal/platform/webui/embedded/client',
  fallback: 'index.html',
})
```

```go
// internal/platform/webui/webui.go
//go:embed all:embedded
var embedded embed.FS
```

Three details matter:

- **`all:` is required.** SvelteKit writes hashed bundles into `_app/`. Without the `all:` prefix, `go:embed` skips every path whose name starts with `_` or `.`, and the application would serve an `index.html` referencing files that are not in the binary.
- **The pattern names `embedded/`, not `embedded/client/`.** The static adapter deletes its output directory before each build, so a placeholder committed inside `client/` would be destroyed by `pnpm build` and would show up as a working-tree change after every build.
- **`embedded/placeholder.html` is committed.** It is why `go build ./...` succeeds in a fresh clone where `pnpm build` has never run: the embed pattern matches at least one file. When `embedded/client/index.html` is missing, the application serves the placeholder in development and refuses to start in production.

`.gitignore` therefore contains `/internal/platform/webui/embedded/client/` alongside `frontend/node_modules/` and `frontend/.svelte-kit/`.

## Generated code and its generators

| Artifact | Path | Generator | Owner |
|---|---|---|---|
| Strict chi server bindings | `internal/platform/httpapi/openapigen/` | oapi-codegen | `[nise]` |
| Typed SQL access | `internal/features/<feature>/store/` | sqlc | `[nise]` |
| TypeScript models | `frontend/src/lib/api/schema.d.ts` | openapi-typescript | `[nise]` |
| Built SPA | `internal/platform/webui/embedded/client/` | `pnpm build` | `[nise]`, not committed |
| sqlc configuration | `sqlc.yaml` | nise | `[nise]` |
| Project recipe | `nise.json` | nise | `[nise]` |

All three code generators write their own `Code generated … DO NOT EDIT.` headers, which satisfies the ownership-header rule for their output.

## Go targets

`frontend/` sits inside the Go module, so `go build ./...`, `go list ./...`, and `gofmt -l .` walk `frontend/node_modules/`, and an npm package that happens to ship a `.go` file is listed as a package of the application. Generated tooling names its Go targets explicitly instead:

```sh
go build ./cmd/... ./internal/... ./db/...
gofmt -l cmd internal db
```

## Naming rules

- Feature directories are singular lowercase nouns and match their Go package name: `internal/features/invoice`. The REST collection (`/api/v1/invoices`), the database table (`invoices`), and the route group (`(app)/invoices/`) are plural.
- Import a feature's `store` package as `<feature>store` when more than one is in scope.
- Migrations are `NNNNN_snake_case.sql` with a zero-padded sequential prefix. Timestamped filenames are not used, because generation must be byte-identical on every machine ([ADR 0002](adr/0002-deterministic-generation.md)).
- Go directory names contain no underscores or hyphens.
- Svelte components are PascalCase. SvelteKit route files keep their `+page.svelte` and `+layout.svelte` conventions.
- No two paths differ only by case; Windows and default macOS filesystems cannot hold both.

## What Slice 1 generates

Slice 1 delivers a project that boots. `nise new` writes the root files, `api/`, `cmd/`, `db/`, `internal/app/`, `internal/platform/{config,database,httpapi,webui}/`, `deploy/`, and a minimal SvelteKit project under `frontend/` that installs, builds, type-checks, and serves one page.

The full authenticated application shell — sidebar, navigation, theme and language controls, authentication screens, tables, and the component set described in [Generated frontend](frontend.md) — arrives with milestone M6. The directories above are where it lands; nothing in this layout moves when it does.

`internal/features/` is created holding only a `.gitkeep`, because version control cannot carry an empty directory. It is populated by `nise generate` from Slice 2 onward.
