# modules

Compile-time optional modules recorded in the project recipe and wired with explicit generated code. The V0.1 list is fixed in `DECISIONS.md`: organizations/RLS, TOTP, notifications (+SSE), uploads/storage.

No dynamic loading and no new modules without a decision record.

Populated by Slices 3–5.
