# test

Cross-cutting fixtures and conformance tests that do not belong to a single package: disposable PostgreSQL harness, generated-tree golden snapshots, determinism and no-network proofs.

Package-level tests stay next to their packages.

Populated by Slice 1 (M1-009, M1-010) and Slice 2 (M3-006).
