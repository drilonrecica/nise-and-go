# templates

Golden-profile source templates consumed by `internal/generator` to produce a new application and its features.

Templates are Nise-owned; their output becomes application-owned. There is no template-override or overlay API in V1.

Populated by Slice 1 (M1-008).
