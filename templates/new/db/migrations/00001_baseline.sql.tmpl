-- Generated once by nise; owned by this application. Nise will not overwrite it.
--
-- This deliberately schema-neutral migration establishes a versioned baseline.
-- Add application tables in later, forward-only migrations.

-- +goose Up
SELECT 1;

-- +goose Down
SELECT 1;
