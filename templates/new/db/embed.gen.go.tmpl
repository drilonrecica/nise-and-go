// Code generated by nise; DO NOT EDIT.

// Package db exposes this application's readable SQL migrations as an
// immutable filesystem embedded in every application binary.
package db

import (
	"embed"
	"io/fs"
)

// embeddedMigrations contains SQL files only. Documentation and editor
// artifacts under db/migrations can never become executable input.
//
//go:embed migrations/*.sql
var embeddedMigrations embed.FS

// Migrations returns a filesystem rooted at db/migrations. The fixed embed
// pattern and fixed subdirectory make failure impossible for generated code;
// panic keeps a future generator regression from being silently ignored.
func Migrations() fs.FS {
	migrations, err := fs.Sub(embeddedMigrations, "migrations")
	if err != nil {
		panic("nise: embedded migration directory is missing: " + err.Error())
	}
	return migrations
}
