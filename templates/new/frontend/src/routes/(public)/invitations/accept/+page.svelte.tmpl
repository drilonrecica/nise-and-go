<!-- Generated once by nise; owned by this application. Nise will not overwrite it. -->
<script lang="ts">
	import * as v from 'valibot';

	import { goto } from '$app/navigation';
	import { page } from '$app/state';

	import { api } from '$lib/api/client';
	import Button from '$lib/components/ui/Button.svelte';
	import Field from '$lib/components/ui/Field.svelte';
	import Input from '$lib/components/ui/Input.svelte';
	import ProblemAlert from '$lib/components/ui/ProblemAlert.svelte';
	import { createForm, mustMatch, rules } from '$lib/forms.svelte';

	// The invitation token is in the link somebody was emailed. Nothing looks
	// it up before the form is submitted: an endpoint that told a holder
	// whether a token was real would turn the link into a guessing oracle, so
	// the first and only answer comes from spending it.
	const token = $derived(page.url.searchParams.get('token') ?? '');

	const schema = v.pipe(
		v.object({ password: rules.newPassword, confirmation: v.string() }),
		mustMatch('password', 'confirmation', 'The two passwords do not match.')
	);

	const form = createForm({
		schema,
		initial: { password: '', confirmation: '' },
		async onsubmit({ password }) {
			const accepted = await api.post('/invitations/accept', { body: { token, password } });
			form.reset();
			// Straight to sign-in with the address filled in. Enrollment does
			// not hand out a session: doing so would make the invitation token
			// a way to obtain one without ever presenting the password that
			// was just set.
			await goto(`/sign-in?email=${encodeURIComponent(accepted.email)}`);
		}
	});
</script>

<svelte:head>
	<title>Accept your invitation · {{.AppName}}</title>
</svelte:head>

<div class="rounded-lg border border-line bg-surface p-6 shadow-raised">
	<h1 class="text-lg font-semibold tracking-tight text-text">Choose a password</h1>
	<p class="mt-1 text-sm text-text-muted">
		This finishes your invitation and creates your account. The link works once.
	</p>

	{#if form.failure}
		<div class="mt-4">
			<ProblemAlert error={form.failure} />
		</div>
	{/if}

	<form class="mt-5 space-y-4" onsubmit={form.submit}>
		<Field
			id="new-password"
			label="Password"
			description="At least 12 characters, and not one that has appeared in a known breach."
			error={form.errors.password}
			required
		>
			{#snippet children({ describedBy, invalid })}
				<Input
					id="new-password"
					type="password"
					autocomplete="new-password"
					bind:value={form.values.password}
					onblur={() => form.touch('password')}
					{describedBy}
					{invalid}
				/>
			{/snippet}
		</Field>

		<Field
			id="confirm-password"
			label="Confirm password"
			error={form.errors.confirmation}
			required
		>
			{#snippet children({ describedBy, invalid })}
				<Input
					id="confirm-password"
					type="password"
					autocomplete="new-password"
					bind:value={form.values.confirmation}
					onblur={() => form.touch('confirmation')}
					{describedBy}
					{invalid}
				/>
			{/snippet}
		</Field>

		<Button
			type="submit"
			variant="primary"
			size="lg"
			loading={form.pending}
			loadingLabel="Creating your account"
			disabled={!token}
			class="w-full">Create account</Button
		>
	</form>
</div>
