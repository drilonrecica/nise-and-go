<!-- Generated once by nise; owned by this application. Nise will not overwrite it. -->
<script lang="ts">
	import AppShell from '$lib/components/shell/AppShell.svelte';
	import { session } from '$lib/session.svelte';

	let { children } = $props();

	// The layout load has already refused to render this group without a
	// session, so account is non-null in practice; the shell still renders
	// both states, because a session that ends while the page is open makes
	// this null before the next navigation notices.
	const account = $derived(
		session.account ? { name: session.account.email, email: session.account.email } : null
	);
</script>

<AppShell name="{{.AppName}}" {account}>
	{@render children()}
</AppShell>
