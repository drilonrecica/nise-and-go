<!-- Generated once by nise; owned by this application. Nise will not overwrite it. -->
<script lang="ts">
	import ThemeControl from '$lib/components/ThemeControl.svelte';
</script>

<svelte:head>
	<title>Settings · {{.AppName}}</title>
</svelte:head>

<div class="mx-auto max-w-3xl px-4 py-8 sm:px-6">
	<h1 class="text-2xl font-semibold tracking-tight text-text">Settings</h1>
	<p class="mt-3 text-sm text-text-muted">
		The second navigation section, so the shell's grouping and its current-section highlighting are
		visible from the first run rather than only after a resource exists.
	</p>

	<section class="mt-8 rounded-lg border border-line bg-surface p-5 shadow-raised">
		<h2 class="text-sm font-medium text-text">About this deployment</h2>
		<p class="mt-1 text-sm text-text-muted">
			The running binary's version, the framework it was built against, and the optional modules it
			carries. Needs the
			<code class="rounded-xs bg-surface-muted px-1 font-mono text-[0.8125rem]">system.read</code>
			permission.
		</p>
		<a
			class="mt-3 inline-block rounded-xs text-sm font-medium text-primary underline underline-offset-4 hover:text-primary-hover"
			href="/settings/about">Open About</a
		>
	</section>

	<section class="mt-6 rounded-lg border border-line bg-surface p-5 shadow-raised">
		<h2 class="text-sm font-medium text-text">Appearance</h2>
		<p class="mt-1 text-sm text-text-muted">
			Light, dark, or follow the operating system. The choice is stored in this browser.
		</p>
		<div class="mt-4">
			<ThemeControl id="settings-theme" />
		</div>
	</section>
</div>
