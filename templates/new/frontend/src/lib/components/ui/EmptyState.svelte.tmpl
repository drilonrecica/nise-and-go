<!-- Generated once by nise; owned by this application. Nise will not overwrite it. -->
<script lang="ts">
	import type { Snippet } from 'svelte';

	import Icon from '$lib/components/ui/Icon.svelte';
	import type { IconName } from '$lib/components/ui/icons';

	let {
		icon = 'list',
		title,
		description,
		/** The one thing to do about it, when there is one. */
		action
	}: {
		icon?: IconName;
		title: string;
		description?: string;
		action?: Snippet;
	} = $props();
</script>

<!--
	Empty is a state, not an absence. A list that renders nothing when it has
	no rows is indistinguishable from one that failed, one that is still
	loading, and one whose filter matched nothing — and a person cannot tell
	which of those to act on.

	The icon is decorative: the heading carries the meaning, so a screen reader
	is not told about a drawing of a list.
-->
<div class="flex flex-col items-center rounded-lg border border-dashed border-line-strong px-6 py-12 text-center">
	<Icon name={icon} class="size-8 text-text-subtle" />
	<p class="mt-3 text-sm font-medium text-text">{title}</p>
	{#if description}
		<p class="mt-1 max-w-sm text-sm text-text-muted">{description}</p>
	{/if}
	{#if action}
		<div class="mt-4">{@render action()}</div>
	{/if}
</div>
