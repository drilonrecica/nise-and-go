// Generated once by nise; owned by this application. Nise will not overwrite it.
//
// The require block is pinned rather than resolved: generation performs no
// network access at all. Run `go mod tidy` once to record go.sum.

module {{.ModulePath}}

go {{.GoVersion}}

require (
	{{.NiseModule}} {{.NiseVersion}}
	github.com/go-chi/chi/v5 {{.ChiVersion}}
)
