-- Generated once by nise; owned by this application. Nise will not overwrite it.
--
-- Immutable release-schema fixture for the generated baseline at version 1.
-- Keep this snapshot byte-stable after release; add a later versioned fixture
-- when the next supported release schema is published.

CREATE TABLE goose_db_version (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    version_id bigint NOT NULL,
    is_applied boolean NOT NULL,
    tstamp timestamp NOT NULL DEFAULT now()
);

INSERT INTO goose_db_version (version_id, is_applied)
VALUES (0, true), (1, true);
