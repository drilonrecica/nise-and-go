// Code generated by nise; DO NOT EDIT.

package app

// SelectedModules returns the compile-time modules recorded in nise.json,
// sorted.
//
// This file is the one regenerated file in internal/app. It holds the
// module selection and the constructor calls that wire it, and nothing
// else, so that changing the selection can rewrite the whole file without
// destroying anything this application wrote. There is no registry, no
// init() registration, and no reflection-driven discovery: a module that is
// not selected contributes no import, no file, and no dead code.
func SelectedModules() []string {
{{- if .Modules }}
	return []string{
{{- range .Modules }}
		{{ printf "%q" . }},
{{- end }}
	}
{{- else }}
	return []string{}
{{- end }}
}
