# internal/cli

CLI commands and terminal presentation: command registration, output modes (plain, color, quiet, verbose, JSON), terminal capability detection, and error rendering.

Private to this module. Applications never import it.

Populated by Slice 1 (M1-002 onward).
